{
  "name": "line-holiday-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LINE bot for routine holiday registration and Google Sheets sync.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --test",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy --keep-vars"
  },
  "devDependencies": {
    "wrangler": "^4.101.0"
  }
}
